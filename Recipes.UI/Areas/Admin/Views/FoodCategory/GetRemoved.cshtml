@model FoodCategoryIndexViewModel
@{ ViewBag.Title = "Silinmiş Kategoriler"; }

<ol class="breadcrumb mb-3 mt-2">
    <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Panel</a></li>
    <li class="breadcrumb-item active">Silinmiş Kategoriler</li>
</ol>

<div class="card mb-4">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table" id="homeDataTable" width="100%" cellspacing="0">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Kategori Adı</th>
                        <th>Ekleyen Kullanıcı</th>
                        <th>Kategori Fotoğrafı</th>
                        <th>Kategori Açıklaması</th>
                        <th>İşlemler</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var category in Model.FoodCategoriesResult.Data)
                    {
                        <tr>
                            <th>@category.Id</th>
                            <td>@category.CategoryName</td>
                            <td>@category.UserName</td>
                            <td><img src="~/Pictures/foodCategoryImgs/@category.CategoryPicture" class="table-image" /></td>
                            <td>@category.CategoryDescription</td>
                            <td>
                                <button class="btn btn-sm btn-outline-success" id="UnDelete" data-id="@category.Id">Geri Yükle</button>
                                <button class="btn btn-sm btn-outline-danger">Kalıcı Sil</button>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/AdminPages/js/dataTableTurkish.js" type="text/ecmascript"></script>
    <script src="~/AdminPages/js/deletedFoodCategoryIndex.js" type="application/ecmascript"></script>
}